import Material from "./Material";

/**
 * interface for StorageIndex, an entity of storage array
 *
 * @export
 * @interface StorageIndex
 */
export default interface StorageIndex {
  material: Material;
  count: number;
}
